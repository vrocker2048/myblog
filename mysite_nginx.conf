
	server {
    	# the port your site will be served on
    	listen      80;
    	# the domain name it will serve for
    	server_name 39.105.180.98; # substitute your machine's IP address or FQDN
    	charset     utf-8;

    	# max upload size
    	client_max_body_size 75M;   # adjust to taste

    	# Django media
    	#location /media  {
        # /home/breavo/PyWorkSpace/mysite_code_shuffle/meida;
       # alias /path/to/your/mysite/media; # your Django project's media files - amend as required
    	#}

    	location /static {
        # /home/breavo/PyWorkSpace/mysite_code_shuffle/static
        root /home/admin/mysite/;
	#alias /home/admin/mysite/static/; # your Django project's static files - amend as required
    	}

    	# Finally, send all non-media requests to the Django server.
    	location / {
        include uwsgi_params;
	uwsgi_pass 0.0.0.0:8001;
    	}
	}


